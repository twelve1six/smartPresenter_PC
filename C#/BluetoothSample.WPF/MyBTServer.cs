using System;
using System.IO;

namespace BluetoothSample.Views {

    public class MyBTServer  {

       // PowerpointController _document;

        /*
        public MyBTServer(PowerpointController document, int port)
            : base(port)
        {
            _document = document;
        }

        bool disposed = false;


        public void pptAction(data data) { 
            //start Slide
            if (data == 1) {
                _document.StartShow(1);
            }

            //move Slide
            else if (data == 2) {
                _document.SetCurrentSlide(data);
               
            }
        }
        */
    }        
}